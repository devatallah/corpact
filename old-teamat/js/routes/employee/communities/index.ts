import preferredProviders from './preferred-providers'
import leagues from './leagues'

const communities = {
    preferredProviders: Object.assign(preferredProviders, preferredProviders),
    leagues: Object.assign(leagues, leagues),
}

export default communities