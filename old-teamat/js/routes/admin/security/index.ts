import events from './events'
import permissionReview from './permission-review'

const security = {
    events: Object.assign(events, events),
    permissionReview: Object.assign(permissionReview, permissionReview),
}

export default security