<?php

/*
 * A11 — العمولة والتسويات ورسوم النظام والفوترة (H §12.7–§12.10).
 *
 * كل رقم هنا إما منصوص عليه في المواصفة (15 يوماً، اليوم 3، 15 يوماً استحقاق،
 * 7/15/30 للتأخر، ضريبة 15%) أو علم تشغيلي. **قيم العقد نفسها ليست هنا**:
 * رسوم الموظف المفعّل والحد الأدنى الشهري ونسبة عمولة المزوّد من العقد
 * (أعمدة `companies`/`partners` + جدولا `effective_from`) — «لا افتراضات
 * مسموحة» في أرقام العقود.
 */
return [

    /*
     * ⚠️ حاجز إصدار الفواتير الحقيقية (H §12.9 — «قرار ضريبي غير نهائي»).
     *
     * جدول الصفة الضريبية (وكيل على قيمة النشاط، أصيل على العمولة ورسوم
     * النظام) هو توجّه البناء المعتمد، **ويحتاج مراجعة محاسب قانوني قبل إصدار
     * أي فاتورة لعميل حقيقي**. ما دام هذا العلم false تُصدَر الفواتير بوضع
     * `provisional`: تُحسب وتُخزَّن وتُعرض للأدمن، ولا تُقدَّم على أنها مستند
     * ضريبي نهائي ولا تُربط بفاتورة (Fatoora). قلبه إلى true قرار مالك/محاسب
     * لا قرار مطوّر.
     */
    'real_invoices_enabled' => (bool) env('BILLING_REAL_INVOICES_ENABLED', false),

    // ضريبة القيمة المضافة على رسوم النظام — «تُضاف على» لا «شاملة» (H §12.8)،
    // بخلاف أسعار الفعاليات الشاملة للضريبة (H §12.1).
    'vat_rate_percent' => 15,

    'settlement' => [
        // كشف كل 15 يوماً: الفترة الأولى 1–15 والثانية 16–آخر الشهر،
        // ويُولَّد صباح اليوم التالي للفترة (1 و16 الساعة 03:00 — H §20).
        'period_split_day' => 15,
    ],

    'invoice' => [
        // إصدار اليوم الثالث من الشهر التالي، والاستحقاق خلال 15 يوماً.
        'issue_day' => 3,
        'due_days' => 15,
        'serial_prefix' => env('BILLING_SERIAL_PREFIX', 'TMT-INV'),
        // سلسلة مستقلة لحصص الموظفين: بائعها غير بائع رسوم النظام ومعالجتها
        // الضريبية غير معالجتها، فترقيمها لا يُخلط بترقيمها.
        'employee_serial_prefix' => env('BILLING_EMPLOYEE_SERIAL_PREFIX', 'TMT-EMP'),
        // بيانات البائع في الفاتورة — تُملأ من العقد/السجل التجاري لتيمات.
        'seller_name' => env('BILLING_SELLER_NAME', 'تيمات'),
        'seller_vat_number' => env('BILLING_SELLER_VAT_NUMBER'),
    ],

    /*
     * سلّم التأخر عن السداد (H §12.8). القاعدة الحاكمة فوق الأرقام:
     * **الحجب على إنشاء الفعاليات وحده** — لا إيقاف دخول موظف ولا إلغاء
     * فعالية مؤكدة أبداً («الموظف لم يخطئ»).
     */
    'late' => [
        'first_reminder_days' => 7,
        'second_reminder_days' => 15,
        'block_event_creation_days' => 30,
    ],

    /*
     * الصفة الضريبية لكل تدفق (H §12.9 — الجدول المؤقت). تُخزَّن على كل حركة
     * وبند حتى يمكن تعديل المعالجة لاحقاً بلا إعادة بناء.
     */
    'tax' => [
        'activity_value' => ['treatment' => 'agent', 'issuer' => 'provider'],
        'commission' => ['treatment' => 'principal', 'issuer' => 'teamat'],
        'system_fee' => ['treatment' => 'principal', 'issuer' => 'teamat'],
        'coordinator_service' => ['treatment' => 'principal', 'issuer' => 'teamat'],
    ],
];
